import React from 'react'
import Alert from '@mui/material/Alert';

const HolidayRejected = () => {
  return (
    <Alert severity="error">This is an error Alert.</Alert>
  )
}

export default HolidayRejected